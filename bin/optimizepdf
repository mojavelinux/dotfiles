#!/bin/bash

if [ -z $1 ]; then
   echo "Please supply a PDF file to optimize"
   exit 1
fi

FILE=$1
FILE_BASENAME=${FILE%.pdf}
FILE_OPTIMIZED=$FILE_BASENAME-optimized.pdf

gs -sOutputFile=$FILE_OPTIMIZED -sDEVICE=pdfwrite -dCompatibilityLevel=1.6 $FILE < /dev/null

exit 0
